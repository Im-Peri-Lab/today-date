# CURRENT_STATE.md

> **마지막 업데이트: 2026-07-27 (v2)**

## 현재 단계
유지보수 / 점진적 UX 개선 단계.

## 현재 한 줄 요약
카테고리 체계 개편(PR #78~79)·최상위 타입 표시 라벨 rename(PR #80)·활동 실내/실외
필수 속성(PR #81)·다이닝 카드 정보 줄 위치 폴백(PR #82)·활동 추천 위저드 실내/실외
단계(PR #83)에 이어, **Android PWA 스플래시 아이콘에 브랜드 하트 추가(PR #87)** 반영
완료. 기능 기준 커밋 `e382af2`.

## 브랜치 상태
- 현재 작업 브랜치 없음, main 기준 build PASS

## 구현 완료 (누적, 기존 유지)
(기존 항목 전체 유지)
- **(신규) Android PWA 스플래시 아이콘에 브랜드 하트 추가** — `public/icon-192.png`·
  `icon-512.png` 신규 생성(기존엔 manifest가 참조만 하고 실물 파일 없던 상태).
  `BrandHeader.tsx`의 하트 path·그라데이션(`#a855f7`→`#ec4899`)을 좌표·색상 변경 없이
  그대로 재사용, 배경색(`#f5f3ff`)은 manifest 기존값 유지. 캔버스 대비 하트 62% 크기
  (icon purpose 미지정→`any` 기본값 확인 후 60~65% 분기 적용, 중간값 채택).
  정적 이미지, 애니메이션 없음. **Android 전용 — iOS는 스플래시 자체가 manifest
  기반이 아니라(iOS/Chrome 포함 전 브라우저가 WebKit이라 동일 제약) 이번 변경의
  영향을 받지 않음, 범위 제외** (PR #87 squash `e382af2`, 상세 → CHANGELOG 2026-07-27 v2)

## 배포 상태
- 플랫폼: Vercel
- URL: `https://today-date-seven.vercel.app`
- 현재 브랜치: `main` (PR #87 기능 기준 `e382af2`)

## 진행 중 / 남은 작업
- 활동 추천 위저드 실내/실외 단계(PR #83) 실제 Supabase 환경 브라우저 클릭 테스트 — 미실행
- Android 스플래시 하트(PR #87) 실기기 홈 화면 추가 후 확인 — 이 세션 환경 제약으로 미실행,
  PNG 파일 육안 확인만 완료
- "저장하고 계속 등록하기" 후속 — 복사하기 흐름 returnTo 배선 통합 검토
- Galaxy 실기기 QA: 하드웨어 미확보로 보류 중
- (이하 기존 항목 유지)

## 알려진 이슈 · 기술 부채
- iOS PWA 스플래시 화면에는 하트 미반영 (iOS는 manifest 기반 자동 스플래시 생성을
  지원하지 않음 — apple-touch-startup-image 등 별도 이미지 세트가 필요한 별개 작업.
  현재 스코프 아님, 향후 필요 시 별도 브랜치)
- (이하 기존 항목 유지)
