# Today Date — PROJECT_CONTEXT UPDATED

> **마지막 업데이트: 2026-07-02**

> 이 문서는 Today Date 프로젝트의 장기 컨텍스트입니다. 260531~260702 전체 핸드오프를 다시 읽고, 초기 컨텍스트에서 이후 작업으로 변경·확정된 내용을 반영한 최신본입니다. 새 AI에게는 이 파일과 `CURRENT_STATE.md`를 먼저 전달하고, 과거 의사결정 확인이 필요할 때만 `CHANGELOG.md`를 참고하게 하세요.

---

## 1. 프로젝트 개요

- **제품명**: Today Date
- **한 줄 요약**: 커플 2인용 데이트 위시리스트 + 추천 PWA
- **구조**: 계정 없는 단일 공유 워크스페이스
  - 최초 설정: 이메일 인증 + **6자리 패스코드** 생성
  - 이후 접근: **6자리 패스코드** 잠금 해제
  - 이메일은 인증·복구 용도
- **핵심 가치**: 둘만의 데이트 후보를 쌓고, 상황에 맞게 활동/장소를 추천받는 모바일 중심 앱
- **현재 제품 단계**: 시제품/MVP에 가까운 개인 프로젝트. 기능 확장보다 일관된 UX·디자인 시스템 정립을 우선해 왔음.

---

## 2. 기술 스택과 주요 위치

### 기술 스택

- Next.js 14 App Router
- TypeScript
- Tailwind CSS
- shadcn/ui
- Supabase DB
  - Service-Role-only 접근
  - RLS 없음
  - 사용자별 행 분리 없음
- Resend 이메일 인증
- iron-session 세션
- React Query
- lucide-react 아이콘
- nextjs-toploader
- 날짜 입력: **네이티브 `<input type="date">`** — 커스텀 date picker 재도입 금지

### 주요 위치

- 로컬 경로: `~/Claude_Project/today-date`
- 실제 로컬 루트 표기: `/Users/sehee/Claude_Project`
- GitHub: `https://github.com/Im-Peri-Lab/today-date`
- Production: `https://today-date-seven.vercel.app`
- Supabase 프로젝트 ID: `xxqfiipzljkjstkgtawj` Sydney `ap-southeast-2`

### 환경변수 이름

실제 값은 핸드오프 문서에 쓰지 않는다.

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `RESEND_API_KEY`
- `RESEND_FROM_EMAIL`
- `SESSION_SECRET`
- `NEXT_PUBLIC_APP_URL`

---

## 3. 역할 분담과 작업 방식

이 프로젝트에서 ChatGPT는 **오케스트레이터/디자인 어드바이저** 역할이다. 코드를 직접 장황하게 작성하기보다, 사용자가 Claude Code CLI에 붙여넣을 **한국어 자연어 프롬프트**를 만든다.

### 역할 분담

- 사용자: 제품/디자인 의사결정자
  - 시안과 스크린샷을 보고 최종 판단
  - 이상한 점을 짚고 기존 토큰 재사용을 강제
  - 점진 적용·브랜치 분리를 요구
- ChatGPT/Claude 어드바이저: 디자인·제품 판단 + Claude Code 프롬프트 작성
  - 추측 대신 진단 프롬프트 제시
  - 화면/상태/토큰 기준을 구조화
  - 핸드오프 문서 작성·갱신 (docs/handoff/ 저장소 보관). 갱신 방식: 어드바이저가 변경 반영한 전문을 작성 → 사용자가 저장소 파일에 덮어쓰기 → Claude Code는 검증 후 커밋만(내용 판단·수정·압축 금지). 각 문서 상단에 "마지막 업데이트 일시" + 범위/기준일을 최신으로 유지.
- Claude Code: 실제 구현·검증·git 작업

### 사용자 상황과 응답 톤

- 사용자는 비개발자에 가깝고 MacBook/PC를 사용한다.
- Claude Code 결과를 스크린샷으로 보고한다.
- 설명은 짧고 명확해야 한다.
- 빈말, 과한 칭찬, 추측성 단정은 금지한다.
- 디자인 판단은 솔직하게 말하되, 사용자 관찰을 우선 신뢰한다.
- 사용자가 “쪼개서 주지 마”라고 하면 관련 작업은 가능한 한 한 프롬프트로 묶는다. 단, 진단 결과 없이는 방향을 정할 수 없는 경우만 분리한다.

### 프롬프트 작성 규칙

- 모든 프롬프트, 명령어, 코드는 마크다운 코드블록으로 감싼다.
- 앱 라우트는 반드시 backtick으로 감싼다.
  - 예: `` `/list` ``, `` `/activities/new` ``
  - 이유: Claude Code가 `/list`를 슬래시 명령어로 오인할 수 있음
- 작업 프롬프트에는 항상 다음을 포함한다.
  - 작업 목적
  - 변경 범위
  - 기능/데이터 변경 금지 여부
  - 디자인 토큰 참조 방식
  - 검증 기준
  - commit + push 지시
- 디자인 갈림길에서는 선택지를 제시하고, 필요하면 디자이너 관점의 최적안을 분명히 권고한다.

### 표준 프롬프트 형식

```md
[작업 제목]. 스타일만, 기능/데이터/레이아웃 변경 금지(해당 시). 앱 라우트는 backtick 표기.
스타일 토큰은 `.claude/skills/today-date-design/SKILL.md`에서 읽어 적용. 최소 수정.

[배경]
...

[작업]
...

[중요]
...

[검증]
- `npx next build` 또는 `npm run build` 통과
- 모바일/데스크탑 확인
- 라이트/다크 확인
- 필요한 경우 computed value·스크린샷·픽셀 샘플링 확인

작업 후 `[브랜치명]` 브랜치에 commit + push.
커밋: "design: ..."
```

### 새 브랜치 시작 표준 프롬프트

```md
새 브랜치 `[브랜치명]`를 main에서 생성·체크아웃.
`.claude/skills/today-date-design/SKILL.md`가 보이는지 확인하고, 없으면 main을 merge해 끌어올 것.
준비되면 한 줄 보고.
```

### 머지 표준 프롬프트 (CLI squash 풀 플로우)

Claude Code가 PR 생성 → squash 머지 → 원격·로컬 정리를 한 번에 처리한다. squash 명시로 배포 목록 통제 의도는 유지한다(fast-forward·일반 merge 금지).

```md
`[브랜치명]` 브랜치를 main에 머지하고 정리까지 한 번에 처리해줘.

1. 브랜치가 모두 commit·push 됐는지 확인
2. main 대상으로 PR 생성 (gh pr create)
   - PR 제목: "[타입]: [요약]"
   - PR 본문에 변경 요약 간단히
3. 반드시 squash 방식으로 머지 (gh pr merge --squash --delete-branch)
   - 일반 merge·fast-forward 금지, squash만
4. 머지 후 로컬 main checkout → pull → 로컬 브랜치 삭제

보고: PR URL / 머지 방식(squash 확인) / 최종 main 반영 여부
```

---

## 4. Git / 배포 운영 원칙

- 작업 단위별로 브랜치를 만든다.
- 미세조정 중간본은 main에 올리지 않는다.
- 브랜치 preview로 확인한다.
- 한 화면/기능/리팩토링 덩어리가 승인되면 main에 머지한다.
- main push 후 Vercel production 반영을 확인한다.
- 브랜치 성격이 다르면 분리한다.
  - 예: 통계 카드 리팩토링과 앱 전체 타이포 표준화는 분리
- 커밋 컨벤션:
  - `feat:` 기능
  - `fix:` 버그 수정
  - `design:` 화면/스타일
  - `refactor:` 구조 개선
  - `docs:` 문서
  - `chore:` 정리
  - `revert:` 되돌리기

### 진단 우선 원칙

원인이 명확하지 않으면 바로 수정 프롬프트를 주지 않는다.

먼저 다음 중 하나를 시킨다.

- 코드로 진단만, 수정 금지
- 정상 케이스 vs 문제 케이스 비교
- git diff로 회귀 추적
- computed value 실측
- 실제 스크린샷 확인
- 같은 역할 요소의 기존 표준·토큰 감사

특히 픽셀, hover, focus, 색상, 높이, 토큰 문제는 눈대중으로 단정하지 않는다.

---

## 5. 구현 완료 범위

### 인증 F-01

- `/setup` 이메일 → 인증메일 → 6자리 패스코드 설정
- `/setup/verify?token=...`
- `/lock` 6자리 패스코드 키패드
- 5회 실패 시 10분 잠금
- `/forgot`
- `/reset?token=...`
- `/api/auth/*`
- 미들웨어:
  - 미설정 → `/setup`
  - 미인증 → `/lock`

### CRUD / 리스트 F-02~F-04

- `/api/activities`, `/api/activities/[id]`
- `/api/places`, `/api/places/[id]`
- `/api/activity-categories`
- `/api/place-categories`
- `/activities/new`
- `/places/new`
- `/list`
  - 활동/장소 탭
  - 위시리스트/다녀온 곳 토글
  - 검색
  - 필터
  - 카드
  - FAB

### 추천 F-05~F-06

- `/api/recommend/activity`
- `/api/recommend/place`
- `/recommend/activity`
- `/recommend/place`
- 추천 로그: `recommendations_log`
- 추천 알고리즘:
  - 필터 + 점수 + top-shuffle
  - `pickTopWithShuffle(scored, 5, 3)`: 점수순 정렬 → 상위 5 → Fisher-Yates 셔플 → 3개 반환
  - 결과는 동등 순위이며 1/2/3 랭킹처럼 보이면 안 됨
  - 후보 풀이 3개보다 많을 때만 “다른 추천 보기” 노출
  - API 응답에 `poolSize` 포함

### 시드 데이터

- 활동 12개
- 장소 14개
- 일회성 삽입 후 seed 스크립트 삭제됨

---

## 6. 디자인 시스템의 단일 출처

모든 화면 디자인의 단일 출처는 Claude Code 스킬이다.

- 위치: `.claude/skills/today-date-design/SKILL.md`
- 역할:
  - 색상
  - 간격
  - 아이콘
  - 블록 구조
  - 버튼/상태 위계
  - 로딩 패턴
  - 포커스 정책
  - 타이포 스케일

### 중요 원칙

- 새 화면 작업 전 반드시 스킬 문서를 읽게 한다.
- 색상이나 간격을 임의로 추가하지 않는다.
- 새 규칙은 먼저 실제 코드로 검증한 뒤 SKILL.md에 문서화한다.
- 새 브랜치가 오래된 main에서 갈라졌다면 SKILL.md가 없을 수 있으므로 main을 먼저 merge한다.
- 문서가 권위가 아니라 **실제 코드와 실측값이 권위**다. 문서와 코드가 충돌하면 코드 실측 후 문서를 갱신한다.
- `docs/design-token-audit.md`는 스냅샷이지 권위 스펙이 아니다.

---

## 7. 핵심 디자인 철학

### 전체 컨셉

- 미니멀 럭셔리
- 로맨틱 한 스푼
- 모바일 중심
- 귀엽지만 유치하지 않게
- 시스템 이모지 남발 금지
- lucide-react 단색 아이콘 중심

### 색상·토큰·위계 (→ SKILL.md 참조)

구체 hex·토큰값·클래스·표는 SKILL.md가 단일 출처다. 여기서는 어디를 볼지만 안내한다.

- 배경색(페이지/카드/컨트롤)·라이트 라벤더 그라데이션(`--s-page-gradient-light`, 일반 화면 전역 적용)·전역 시맨틱 토큰(`:root`) → **SKILL §1**
- 글씨색 3단 위계(`--s-ink` > `--s-sub` > `--s-faint`)·sub/faint 분리 원칙 → **SKILL §2·§2-C**
- 라이트/다크 처리(`@media` + `--s-*`, shadcn `.dark` 미사용, portal은 `:root` 상속) → **SKILL §0·§2·금지규칙 12**
- 라이트 카드 원칙(흰 면 + 연한 보더)·카드 그림자 3단 위계(추천>리스트>통계 `--s-card-shadow-xs`) → **SKILL §1·§3**
- 카드/아이콘 원칙(`rounded` 좌측정렬·카테고리 뱃지 배경 없음·이모지 금지·표시용 매핑) → **SKILL §3·§7·§8**

---

## 8. 상호작용 / 포커스 / 상태 시스템

선택/활성 공통 언어(보라 테두리)·포커스 글로우 4-tier(`--s-focus-ring` 2px / `--s-focus-ring-card` 3px / `--s-active-glow` 입력 전용 / 인라인 링크는 밑줄)·적용 판정 기준·중성 보조 버튼(iconBtn·editGhostBtn 등 중성 hover)의 구체 스펙은 SKILL.md가 단일 출처.

- 포커스·활성·hover·눌림 표준 → **SKILL §5**
- 상태 표현 위계(단색=CTA 하나 / 선택=틴트) + hover 언어(콘텐츠=accent, 유틸=중성) → **SKILL §5-A**

---

## 9. 버튼 / 컨트롤 원칙

컨트롤 높이(40/36/48px)·액션 버튼 2종 패턴(새 데이터=풀폭 Primary 48px / 그 외=콘텐츠폭)·위시·다녀온 토글·칩 선택·입력 텍스트(16px, 폼 라벨 14px)·`mapActionBtn`(28px)의 구체 스펙은 SKILL.md가 단일 출처.

- 컨트롤 높이·폼 입력/선택·날짜 → **SKILL §4·§4-A**
- 액션 버튼 2종 패턴·토글·칩 선택·상태 채움 위계 → **SKILL §4-A·§5-A**
- 유틸 아이콘 글리프 크기·`mapActionBtn` 28px → **SKILL §7**

---

## 10. 인증 화면 규칙

### 패스코드

- PasscodeInput은 setup/reset/lock 모두 6자리 고정
- 6자리 입력 전 `onComplete`가 발화하지 않음
- API 검증도 `min(6).max(6)`으로 정합화됨
- reset/setup 안내 문구도 “6자리”로 통일
- 더 이상 “4~6자리”라고 쓰면 안 됨

### 로딩 / fallback

- `setup`·`reset`은 `useSearchParams()` 때문에 Suspense가 필요하다.
- `fallback={null}` 금지. fresh load에서 흰 화면이 생긴다.
- fallback은 `AuthLayout subtitle="..."` 껍데기를 사용한다.
- setup fallback subtitle: “둘만의 공간을 만들어요”
- reset fallback subtitle: “새로운 6자리 패스코드를 설정하세요”

### 높이 정책

인증 화면은 `100dvh`(일반 화면 `svh`와 분리 유지). 구체 스펙·회귀 교훈 → **SKILL §9-A**.

---

## 11. 상세 화면 규칙

대상:

- `/activities/[id]`
- `/places/[id]`

### 상세 화면 디자인 (→ SKILL.md 참조)

2블록 구조·상태 라벨/태그·상태 전환 버튼 문구·인라인 편집·카드 ⋮ 메뉴 항목·별점·날짜·시간대·식사시간 칩의 구체 스펙은 SKILL.md가 단일 출처.

- 2블록(등록 정보/방문 기록) 구조·인라인 편집·연필 버튼·필드 레이아웃 → **SKILL §10-A~§10-C**
- 상태 라벨/태그(`STATUS_LABELS`)·상태 전환 버튼 문구(정방향 "다녀왔어요" / 역방향 "가보고 싶은 곳으로") → **SKILL §10-E·§10-F**
- 카드 ⋮ 메뉴 항목(미방문/방문완료별) → **SKILL §5-A**
- 별점 sm/lg·날짜(네이티브 `<input type="date">`, 표시 `YYYY.MM.DD (요일)`) → **SKILL §10-H·§4-A**
- 시간대(`day`/`night`/`any` 표시·`src/lib/labels.ts` 아이콘 매핑) → **SKILL §8-B**
- 식사시간 칩(`.mealBadge` 정보 뱃지, `--type-caption`·`--s-card-border-strong`, `--s-track` 금지) → **SKILL §8**

### 참고 링크

- 저장값은 사용자 입력 원본 유지(마이그레이션 없음), 표시 텍스트도 입력 원본.
- 여는 href만 `src/lib/url.ts`의 `resolveHref(url)`로 스킴 보정: 스킴 없으면 https:// 부착, http(s)/ftp/mailto/tel은 유지, host:8080 포트 오인 방지.
- 스킴 없는 URL(`naver.com`) 입력 허용. 이를 위해 폼 refine·API 4개 라우트의 `z.string().url()` 검증을 완화하고 `isValidReferenceUrl(url)`(폼·API 공용)로 대체.
- 표시: "참고 링크 열기" 고정 텍스트가 아니라 실제 URL + 한 줄 말줄임(`min-w-0 truncate`). 아이콘 external-link(새 탭 열림 의미).
- 링크 열기 동작 `target="_blank" rel="noopener noreferrer"` 유지.

### area / location 필드 구분

- `area` = 추천 범위용 지역 태그(성수/홍대). places만 보유. 화면 라벨 "지역". 추천 필터에 사용.
- `location` = 지도 검색용 실제 위치(상호명·관광지명·주소 모두 허용). activities·places 둘 다. 화면 라벨 "위치". optional. 추천엔 미사용(표시·지도 열기 전용).
- 두 필드는 성격이 다르므로 절대 합치지 않는다. **재정의 배경**: 추천용 지역 태그와 지도 검색용 실제 위치는 성격이 완전히 다른 두 정보인데 하나의 `location` 필드에 섞여 있었다. 지도 연동을 추가하며 분리 필요성이 표면화 → 260702에 기존 `location`을 `area`(넓은 분류=어느 동네)로 rename하고, `location`(콕 집은 목적지=지도로 열 실제 위치)을 새 의미로 재도입. 마이그레이션 004(rename)·005(신규 컬럼).

### 지도 앱 연동 · 필드 순서 (→ SKILL.md 참조)

- 지도 앱 연동(`lib/map.ts` `MAP_APPS`·`useMapAppPreference` 훅·`MapLink` 컴포넌트·`mapActionBtn` 28px·위치 미입력 빈 상태) → **SKILL §10-I**
- 장소·활동 폼/상세 필드 순서(장소: 식사시간→지역→위치 / 활동: 시간대→위치) → **SKILL §10-J**

---

## 12. 리스트 / 카드 규칙

카드 내부 수직 간격(헤더 덩어리 2px / 경계 12px / 정보 덩어리 4px, ActivityCard·PlaceCard 동기화)·시간대 표시·리스트 로딩(CardGridSkeleton)의 구체 스펙은 SKILL.md가 단일 출처.

- 카드 내부 수직 간격 → **SKILL §8-A**
- 시간대(`time_of_day`) 표시 규칙 → **SKILL §8-B**
- `/list` 로딩 패턴(CardGridSkeleton) → **SKILL §12**

---

## 13. 추천 화면 규칙

### 추천 알고리즘

- 활동 추천과 장소 추천은 분리된 API를 사용한다.
- 방식: 필터 + 점수 + top-shuffle
- 결과는 랭킹이 아니라 동등 추천 묶음이다.
- “다른 추천 보기”는 후보 풀이 반환 개수보다 많을 때만 노출한다.

### 활동 추천

- 시간대
- 소요시간
- 카테고리
- 시간대 매칭:
  - 주간(day)=day+any
  - 야간(night)=night+any
  - 아무때나(any)=전부
- duration:
  - 기본 strict 필터
  - “더 짧은 일정 포함” 옵션
  - “1박 이상” 선택 시 time_of_day 단계 skip

### 장소 추천
- 지역(area)
- 식사 타이밍
- 카테고리
- area는 자유 입력 텍스트 + 부분포함 필터 (추천 범위용 지역 태그)
- 미입력 시 area 필터는 꺼짐
- 참고: location(지도 검색용 실제 위치)은 추천에 사용하지 않는다(표시·지도 열기 전용)

### 입력 단계

- 단일 선택 단계: 선택 즉시 자동 진행
- 카테고리 다중 선택 단계: “추천 받기” 버튼 유지

### 결과 화면 색 위계 (→ SKILL.md 참조)

단색 채움=지금 누를 CTA 하나 / 선택됨=틴트+accent 보더 / 0개 상태 추가 버튼=단독 진입이라 그라데이션 허용. 구체 스펙 → **SKILL §5-A**.

### 결과 하단 버튼

결과가 1개 이상일 때:

1. 더 짧은 일정 토글
2. 다른 추천 보기
3. 처음부터

0개 상태:

- 박스 아래 “다른 추천 보기” 없음
- “처음부터”만 유지

---

## 14. 다이얼로그 규칙

닫기 수단(입력 폼=X만 / 결정=취소만)·제목 위계(전용 20px)·다크 대응(portal 오버레이·팝업 분리)의 구체 스펙은 SKILL.md가 단일 출처.

- 닫기 수단 → **SKILL §11**
- 제목 스케일 → **SKILL §11-A**
- 다크 식별성(오버레이·팝업) → **SKILL §11-B**

---

## 15. 로딩 전략

화면 전환 로딩 패턴(예측 어려운 화면=프로그레스 바/서버 prefetch, 레이아웃 고정=스켈레톤)·전역 프로그레스 바(`nextjs-toploader`)·프로그래매틱 전환(`useTopLoader`)·setup/reset fallback의 구체 스펙은 SKILL.md가 단일 출처.

- 로딩 패턴 선택 기준·프로그레스 바·서버 prefetch·프로그래매틱 전환 코드 패턴 → **SKILL §12**
- 서버 prefetch 공용 함수(코드 위치): `src/lib/data/activities.ts`·`places.ts`·`dashboard.ts`

---

## 16. 홈 통계 카드 규칙

홈 통계 카드 4개는 다음 필터 진입점이다.

- 활동 / 가보고 싶은 곳
- 활동 / 다녀온 곳
- 장소 / 가보고 싶은 곳
- 장소 / 다녀온 곳

각 카드 클릭 시 `/list?tab=activity|place&status=wishlist|visited`로 연결된다.

### 채택한 레이아웃

- 행 리스트형
- 우측 큰 숫자 제거
- 개수는 미리보기 문장에 흡수
  - 예: “수상스포츠 · 자전거 외 8개”
- 빈 상태는 자연어로 표시
  - 예: “아직 다녀온 곳이 없어요”
- 다녀온 곳이고 count > 0일 때만 초록 체크 배지

### 토큰·크기·레이아웃 확정값 (→ SKILL.md 참조)

통계 카드 토큰 정리(전용 토큰 제거→전역 시맨틱 수렴)·제목 크기(모바일 15/PC 16, `.cardTitleSm`)·배경 그라데이션·홈 레이아웃 확정값(모바일 `max-w-xl`/PC `max-w-3xl`·패딩·정렬, 폭 확장 역효과로 768px 유지)의 구체 스펙은 SKILL.md가 단일 출처.

- 토큰 정리·배경 → **SKILL §1·§3**
- 통계 행 제목 크기 → **SKILL §2-A·§2-B**
- 홈 레이아웃 확정값·폭 정책(변경 금지) → **SKILL §3-A**

---

## 17. 타이포그래피 표준

타이포 7토큰 표준화 완료 → 상세는 SKILL §2-B 참조.

### 7토큰 타이포 스케일·라벨 위계 (→ SKILL.md 참조)

7개 역할 타이포 토큰 표(`--type-*`)·유틸 클래스·사용 규칙·라벨 위계(sectionLabel 600 / fieldLabel 500)·상세 값 위계·의도된 예외·토큰만 빌려 쓰는 패턴의 구체 스펙은 SKILL.md가 단일 출처.

- 7토큰 표·유틸 클래스·사용 규칙·의도된 예외 → **SKILL §2-B**
- 라벨 위계(sectionLabel/fieldLabel)·상세 화면 값 위계 → **SKILL §2-C**
- `--type-caption` 토큰만 빌려 쓰기(`.mealBadge`) → **SKILL §8**

### 작업 교훈

타이포 표준화 작업에서 도출된 교훈(토큰 의도 역해석 금지, :root 승격 픽셀 검증, 공용 클래스 용처 감사, 토큰 통합/분리 판단, 진단 수치 재검증, 사용자 관찰 vs 추정 구분)은 앱 전역 원칙으로 일반화되어 §20 알려진 교훈에 통합됨. → §20 참조.

---

## 18. 페이지 높이 정책

일반 화면 `min-height:100svh`(주소창 출렁임 방지) / 인증 화면 `100dvh`(`auth.module.css` 별도 스코프). 전역 높이 단위를 바꿀 때 인증까지 휩쓸지 말 것, 높이는 요소 깎기가 아니라 정렬(`margin:auto`)로 해결. 구체 스펙·회귀 교훈 → **SKILL §9-A**.

---

## 19. Phase 2 / 장기 백로그

> 여기는 "언젠가 할 수 있는 방향"만 담는다. 현재 우선순위·다음 작업·진행 중 백로그는 CURRENT_STATE.md, 완료 이력은 CHANGELOG.md를 본다. 날짜·PR·완료표시는 이 섹션에 남기지 않는다.

### Phase 2 기능 후보

- F-07 사진 업로드 (skip 또는 낮은 우선순위)
- F-08 CSV
- F-09 통계 대시보드
- F-10 카테고리 관리
- F-11 설정 페이지 (이메일/패스코드/커플 정보 변경)
- F-12 PWA 설치
- F-13 Gemini AI 추천
- F-14 데이트 코스 번들
- 다크모드 토글
- 다중 삭제/완료

### 사용자 요청 파생 기능 방향

- 복사 생성 (기존 활동/장소 복사해 새로 생성)
- 다녀온 날짜 기간 입력 (단일→시작~종료, DB 변경 동반, 활동 추천 로직 영향)
- 주소 + 지도 앱 연동 (텍스트 저장 → 구글/네이버맵 검색 URL, DB 변경 동반)
- 검색/필터 URL 동기화 (리스트 조건의 URL 상태화)
- 추가 플로우 returnTo 배선 (리스트→추가→등록→상세→목록 왕복 시 조건 유지)

### 타임리스 기술 백로그 (방향만, 착수 전 실측 필요)

- spacing 토큰 표준화 (`--s-space-*` 부재, 유사 케이스 누적 시 수렴 검토)
- `/list` 서버 prefetch (현재 스켈레톤 유지)
- 진한 배경 Primary 버튼 focus 가시성 (이중 링 검토)
- 다크 hover 2계열 분리 검토
- `.detailDeleteBtn` 라이트 hover 불일치 검토
- 죽은 `dark:` 변형 점검
- 위저드 h1 수렴 검토

---

## 20. 알려진 교훈 / 금지 사항

- 추측으로 픽셀·색·높이를 단정하지 않는다.
- computed value만 보고 끝내지 않는다. 실제 스크린샷과 사람 눈 검증이 필요하다.
- 필요하면 실제 렌더 픽셀 색을 샘플링해 명도차를 본다.
- 사용자 관찰은 높은 신뢰도로 재검토한다.
- 확정 규칙처럼 보이는 것도 사용자 합의 없이 문서에 박힌 경우가 있을 수 있다.
- 여러 번 수정해도 안 잡히면 즉시 진단 모드로 전환한다.
- 라이트 렌더를 바꾸는 토큰 정리는 픽셀 불변 여부를 반드시 확인한다.
- portal 요소는 `.page` 토큰 상속을 받지 못한다.
- 스켈레톤은 모든 로딩의 정답이 아니다.
- 새 규칙은 문서부터 박지 말고 실물 코드로 확인 후 문서화한다.
- “기존 표준부터 검증”이 즉흥 hex와 임의 위계를 막는 핵심 안전장치다.
- 위계는 없음/있음이 아니라 강도 단계로 표현한다.
- 다크모드는 명도 방향이 라이트와 반대다. 다크에서 강조/약화가 역전되지 않는지 별도 확인한다.
- 표준 수립은 전면 재설계가 아니라 기존 값의 역할별 수렴으로 접근한다.
- UI가 막아준다고 구조적으로 안전한 것은 아니다. API 검증도 함께 맞춰야 한다.
- **토큰의 목표값과 현재값이 다른 건 정상이다.** 그 차이를 없애는 게 토큰의 존재 이유이므로, "값이 달라서 수렴 제외"로 역해석하지 않는다.
- **같은 역할로 보여도 계위가 다르면 분리한다.** "라벨" 하나로 묶지 말고 독립 구획 헤더(sectionLabel)와 값에 종속된 필드명(fieldLabel)을 구분한다. 표준화는 무조건 한 클래스로 합치는 게 아니라 역할을 정확히 가르는 것이다.
- **공용 클래스를 통째로 바꾸기 전에 모든 용처를 감사한다.** 한 클래스에 다른 역할이 섞여 있으면 일부만 분리해야 한다.
- **레이아웃 재배분은 한 화면만 보고 결정하지 않는다.** 폭·여백은 다른 화면 영향과 전체 비율을 함께 본다. 한쪽을 살리려 다른 쪽을 깎는 트레이드는 대개 역효과다(추천 축소로 통계 살리기 실패 사례).
- **멀쩡한 것을 더 건드리면 깨진다.** 만족스러운 상태에 도달하면 멈춘다(홈 추천 제목 16px, PC 폭 768px 등).
- **머지는 CLI squash 풀 플로우다.** Claude Code가 PR 생성 → `gh pr merge --squash --delete-branch` → 로컬 main pull·브랜치 삭제를 한 번에 처리한다. squash 명시로 배포 목록 통제(fast-forward·일반 merge 금지).
- **백로그 항목은 실측 전까지 "진짜 빚"으로 단정하지 않는다.** 이전 어시스턴트가 텍스트로만 남긴 stale 항목이 섞여 있다(260630에 입력 step·버튼 max-width 2건이 "코드는 이미 멀쩡"으로 닫힘). 작업 진입 전 진단 프롬프트로 실측 확인 먼저.
- **진단 시 컴포넌트의 렌더 분기를 혼동하지 않는다.** 한 컴포넌트가 조건부로 다른 컨테이너를 쓰면(마법사 화면 512px vs 결과 화면 896px) 어느 분기의 값인지 확인해야 엉뚱한 후보를 잡지 않는다(260630 버튼 max-width 1차 오독 사례).
- **`--s-track`은 세그먼트 "눌린 홈" 전용 색이다.** 카드 위에 얹히는 뱃지/요소 배경에 재사용하면 다크에서 카드면보다 어두워 가라앉는다. 카드 위 정보 뱃지 면색은 `--s-card-border-strong`. 라이트에서 두 토큰 값이 같아도 다크에서 갈라지므로 다크 별도 확인 필수.
- **화면 성격이 다르면 같은 요소라도 폭/크기가 달라도 된다.** 버튼 폭이 질문 512px / 결과 320px / 갤러리 896px로 다른 건 불일치가 아니라 각 화면에 최적화된 의도. 통일이 항상 정답은 아니다.
- **진단이 내놓은 수치도 결론을 뒤집을 만하면 재검증한다.** `--s-sub` 대비를 4.35:1(AA 미달)로 보고했던 게 계산 오류였고 실제 4.83:1(통과)이었다. 접근성·대비처럼 수치가 결론을 좌우하면 계산 과정을 다시 확인한다. "백로그는 실측 전까지 진짜 빚으로 단정 말 것"에 더해, 그 실측 수치 자체도 의심 대상이다.
- **사용자 관찰 ≠ 어시스턴트 추정.** "약하다/흐리다"가 사용자 신호인지 이전 어시스턴트가 추정해 남긴 것인지 출처를 확인한다. 추정을 사용자 신호로 잘못 귀속하면 의도된 위계를 깨는 불필요한 전역 작업으로 번진다. 추정이면 객관 기준으로만 평가하고, 의도된 설계면 근거 기록 후 닫는다.
- **토큰 통합은 사용처 성격이 겹칠 때만.** `--s-hint`(죽은 토큰)는 통합, `--s-sub`/`--s-faint`(성격이 갈리는 살아있는 토큰)는 분리 유지. 합치면 위계가 소멸하는 토큰은 절대 통합하지 않는다.
- **요소에 직접 background를 칠하는 패턴은 `min-height: 100svh`를 동반해야 한다.** 없으면 짧은 콘텐츠에서 배경이 콘텐츠 높이까지만 칠해지고 그 아래가 노출된다(그라데이션 전역 확장 시 `.pageStatic min-height: auto` 버그).
- **회귀 여부는 "언제 보였나"가 아니라 "어디서 비롯됐나"로 판단한다.** 표면화 시점으로 회귀를 오판하지 말고, 진단으로 뿌리(기존 동작인지 신규 유발인지)를 확인한다. (사례 → CHANGELOG)
- **입력을 막는 검증을 먼저 본다.** 링크 스킴 보정은 표시(href)만 고쳐선 안 되고, 애초에 스킴 없는 입력을 차단하던 폼·API zod 검증을 함께 완화해야 실제 동작한다. 표시만 보지 말고 입력 파이프라인 전체를 확인한다.
- **스코프 경계는 진단으로 긋는다.** 무엇이 스코프 안/밖인지 진단으로 확인해 별도 백로그로 분리하고, 한 브랜치에 무리하게 끼우지 않는다. (사례 → CHANGELOG)
- **문구 통일 ≠ 무조건 동일화.** 상세 하단 버튼과 카드 메뉴는 맥락이 달라 정방향 문구가 달라도 된다("다녀왔어요" 유지). 통일의 실제 목적은 역방향 "되돌리기"의 어색함 제거였다. 통일 대상을 정확히 특정한다.
- **미구현 필드는 매핑보다 제거한다.** UI가 끝내 안 붙은 유령 필드(added_by)에 labels.ts 표시 매핑을 넣는 건 없는 계약을 하드코딩하는 것. 필드 자체를 걷어낸다.
- **rename은 전제부터 진단한다.** "activities·places 둘 다"라는 요청 전제가 grep 전수 결과 틀렸다(location은 places만). 착수 전 실제 범위를 코드로 확정한다.
- **같은 이름의 의미가 뒤바뀌면 시간차로 분리한다.** location→area rename을 먼저 완료·배포한 뒤 location을 새 의미로 재도입했다. 한 브랜치에 섞으면 의미 충돌·데이터 사고 위험.
- **확장은 지금 다 만들지 말고 구조만 잡는다.** 지도 앱을 config 배열(`MAP_APPS`)로 빼서 카카오·티맵은 항목 추가만으로 확장. "확장 대비"는 전부 구현이 아니라 싸게 붙일 구조를 만드는 것.
- **아이콘 간격은 박스가 아니라 내부 여백이 지배한다.** 탭타깃(36/44px)을 유지하면 글리프 간격을 CSS gap으로 못 좁힌다. 인라인 액션 쌍 전용 `mapActionBtn`(28px)으로 박스를 줄여 해소.
- **작은 UI 조정도 PR 경유가 원칙이다.** Vercel 배포 목록 관리를 위해 작은 변경도 main 직접 푸시를 지양하고 PR을 경유한다. (사례 → CHANGELOG)

---

## 21. 새 AI가 처음 해야 할 일

1. `PROJECT_CONTEXT.md`를 읽는다.
2. `CURRENT_STATE.md`를 읽는다.
3. 현재 git 상태를 확인한다.
4. `.claude/skills/today-date-design/SKILL.md`를 확인한다.
5. `CURRENT_STATE.md`의 다음 작업을 기준으로 프롬프트를 작성한다.
6. 과거 의사결정이 필요할 때만 `CHANGELOG.md`를 본다.
